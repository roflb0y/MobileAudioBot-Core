okay hi so the bot is dead so heres the source code for it. beware of shitcode. MobileAudioBot consists of two parts: the core and the worker. this is the core part which handles all the discord interaction, works with the database etc. for the sake of scalability the worker part that actually converts videos was separated from the main part. the worker part can be found [here](https://pidor.makeup)
